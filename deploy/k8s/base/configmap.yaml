apiVersion: v1
kind: ConfigMap
metadata:
  name: cistafirma-config
  namespace: cistafirma
data:
  DJANGO_SETTINGS_MODULE: backend.settings
  ALLOWED_HOSTS: "*"
  CORS_ALLOWED_ORIGINS: "https://cistafirma.example.com"
  CSRF_TRUSTED_ORIGINS: "https://cistafirma.example.com"
  BACKEND_PORT: "8000"

